# Frontend Environment Variables

# Backend API URL (without trailing slash)
VITE_API_BASE_URL=http://localhost:8000

# WebSocket URL (without protocol, ws:// or wss:// will be automatically determined)
# For local: localhost:8000
# For production: your-backend-domain.com (without port if using standard HTTPS)
VITE_WS_URL=localhost:8000

# Frontend URL (used for redirects)
VITE_FRONTEND_URL=http://localhost:5173
